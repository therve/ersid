#!/usr/bin/env python

import sys

from twisted.internet import reactor
from twisted.web.server import Site
from twisted.python import log

from ersid.rest import resource

reactor.listenTCP(8080, Site(resource()))

log.startLogging(sys.stdout)
reactor.run()
